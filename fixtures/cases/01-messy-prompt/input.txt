ok so what i need is basically a python script that takes a folder of csv files and merges them but ONLY the ones that have a "date" column, and also it should skip empty files. oh and the output should be a single parquet file. i forgot to mention the csvs are like sales data from different regions so they might have slightly different columns, some have "Region" some have "region_name" so those need to be treated as the same thing. also please use pandas. actually wait, it also needs to handle the case where dates are in different formats, some are MM/DD/YYYY and some are ISO. like i said merge them all into one parquet. also add logging. and it should be fast because there are like 10000 files. did i mention to skip empty files? yeah skip those. one more thing - if a file fails to parse dont crash, just log it and continue. use pandas like i said. the output parquet should be partitioned by year. oh and can you make the date column always called "date" lowercase in the output. thanks!!! also python 3.11
